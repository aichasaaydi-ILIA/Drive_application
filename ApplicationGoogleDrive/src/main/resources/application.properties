# Application
server.port=8080
spring.application.name=ApplicationGoogleDrive

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/drive1_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration

## Solution 1 : Utilisez HS256 (recommandé - plus simple)

**Modifiez `JwtTokenUtil.java` :**
```java
private String doGenerateToken(Map<String, Object> claims, String subject) {
return Jwts.builder()
.setClaims(claims)
.setSubject(subject)
.setIssuedAt(new Date(System.currentTimeMillis()))
.setExpiration(new Date(System.currentTimeMillis() + expiration))
.signWith(SignatureAlgorithm.HS256, secret) // Changé HS512 ? HS256
.compact();
}
```

## Solution 2 : Générez une clé plus longue

**Dans `application.properties` :**
```properties
# Utilisez cette clé (elle fait 64 caractères = 512 bits)
jwt.secret=MySuperSecretJwtKey123456789MySuperSecretJwtKey123456789
jwt.expiration=86400000

# Google OAuth2
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

# CORS
cors.allowed-origins=http://localhost:4200